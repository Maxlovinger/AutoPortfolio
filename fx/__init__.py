"""Currency (FX) model — kept separate from the equity system.

See ../currency_model_proposal.md for the design. This package provides the
data layer (spot FX + short rates → carry base) as build-order steps 1–2.
"""
